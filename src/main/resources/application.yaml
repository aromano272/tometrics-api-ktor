ktor:
    application:
        modules:
            - com.sproutscout.api.ApplicationKt.module
    deployment:
        port: "$PORT:8080"
        watch:
            - classes
    development: true

jwt:
    domain: "sproutscout.com"
    audience: "sproutscout-users"
    realm: "SproutScout"

postgres:
    maxPoolSize: 10